// import { fromJS } from 'immutable';
// import { makeSelectTilesPageDomain } from '../selectors';

// const selector = makeSelectTilesPageDomain();

describe('makeSelectTilesPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
