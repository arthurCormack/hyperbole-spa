// import React from 'react';
// import { shallow } from 'enzyme';

// import SponsoredContainer from '../index';

describe('<SponsoredContainer />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
