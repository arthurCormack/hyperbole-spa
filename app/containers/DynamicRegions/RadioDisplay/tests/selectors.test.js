// import { selectRadioDisplayDomain } from '../selectors';
// import { fromJS } from 'immutable';
// import expect from 'expect';

// const selector = selectRadioDisplayDomain();

describe('selectRadioDisplayDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
