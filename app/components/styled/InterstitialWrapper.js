import styled from 'styled-components';
import { Grid, Col, Row } from 'react-bootstrap';
import { media } from 'style-utils';

export const InterstitialWrapper = styled.div`
  ${'' /* min-height: 200px; */}
`;

 export default InterstitialWrapper;
