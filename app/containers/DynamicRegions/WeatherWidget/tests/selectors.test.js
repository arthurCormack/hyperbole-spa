// import { selectWeatherWidgetDomain } from '../selectors';
// import { fromJS } from 'immutable';
// import expect from 'expect';

// const selector = selectWeatherWidgetDomain();

describe('selectWeatherWidgetDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
