// import { fromJS } from 'immutable';
// import { makeSelectHoroscopesPageDomain } from '../selectors';

// const selector = makeSelectHoroscopesPageDomain();

describe('makeSelectHoroscopesPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
