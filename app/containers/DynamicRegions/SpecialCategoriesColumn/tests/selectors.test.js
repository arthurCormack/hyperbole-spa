// import { fromJS } from 'immutable';
// import { makeSelectSpecialCategoriesColumnDomain } from '../selectors';

// const selector = makeSelectSpecialCategoriesColumnDomain();

describe('makeSelectSpecialCategoriesColumnDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
